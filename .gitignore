# Claude Code ctags index
.claude/tags

# Python
__pycache__/
*.pyc

# Arduino / build output
build/
*.elf
*.bin.hex

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Serial capture scratch files (just monitor-save / deploy / poke)
deploy*.log
poke*.log
probe*.log
session.log

# Playwright MCP scratch
.playwright-mcp/
